# 第三章 目标识别与定位方法 —— 撰写大纲

> 基于现有三份代码文件（`detect_well(5).py`、`detect_num_gps.py`、`save_pic.cpp`）的逐节支撑分析。

---

## 3.1 侦察流程设计

| 子要点 | 对应代码 | 说明 |
|--------|----------|------|
| 整体 pipeline 架构图 | `save_pic.cpp` + `detect_well(5).py` | C++端（GStreamer 采图 → 共享内存） → Python 端（读取 → 检测 → 定位 → 决策 → Socket 发送） |
| 实时 / 离线双模式设计 | `detect_well(5).py`（实时共享内存） vs `detect_num_gps.py`（离线文件读取） | 两套方案的适用场景对比 |
| C++/Python 进程间通信机制 | `save_pic.cpp`（写入端） + `detect_well(5).py`（读取端） | 共享内存 `SharedData` 结构体 + flag 握手协议 |
| 触发拍照的距离 / 航点条件 | `save_pic.cpp` 中 `dis < dis_lim && plane_alt > 10 \|\| current_wp >= circle_wp` | 距离阈值 + 盘旋航点双重触发 |
| ROS 话题订阅与状态获取 | `save_pic.cpp` 中 `gps_cb`、`pos_cb`、`wp_cb` | MAVROS 获取 GPS / 姿态 / 航点 |

---

## 3.2 第一轮图片识别方案

| 子要点 | 对应代码 | 说明 |
|--------|----------|------|
| 检测模型选型 | `detect_well(5).py` L382-383: `YOLO("weights/wellcut_1002.pt")` + `YOLO("weights/round1_1004_tqc.pt")` | 两阶段级联：det 检天井 → cls 分类井内图像 |
| 输入预处理 | `detect_well(5).py`: `well_imgsz=320`, `pic_imgsz=640`, `padding=15`, `resize` | 图像缩放与裁剪策略 |
| 检测参数配置 | `detect_well(5).py`: `well_conf=0.5`, `pic_conf=0.75`, `BATCH_SIZE=6` | 置信度阈值与批处理大小 |
| GPU 加速方案 | `detect_well(5).py`: `det.fuse()`, `model.half()`, `device=0` | 模型融合 + FP16 半精度推理 |
| 天井区域裁剪策略 | `detect_well(5).py` 中 `cut()` 函数 | 以检测框中心扩方、pad 边界外扩、resize 归一化 |
| 分类结果与井的关联 | `detect_well(5).py` 中 `infer_thread()` 的坐标反算 | 子图检测坐标 → 原图像素坐标的映射 |

---

## 3.3 第二轮数字识别方案

| 子要点 | 对应代码 | 说明 |
|--------|----------|------|
| 关键点检测模型 | `detect_num_gps.py` L324: `YOLO("weights/round2_0914.pt")` | 5 关键点（中心 + 4 角点）检测 |
| 关键点顺序判定 | `detect_num_gps.py` 中 `is_angle_greater_than_180_counterclockwise()` | 叉积法判断角点顺 / 逆时针排列 |
| 透视变换矫正 | `detect_num_gps.py`: `cv2.getPerspectiveTransform()` → `cv2.warpPerspective()` | 4 角点 → 100×100 标准正视图 |
| OCR 预处理管线 | `detect_num_gps.py`: 灰度化 → 高斯模糊 → 自适应二值化 → 形态学开运算 → 腐蚀 | 轻量级预处理适配嵌入式平台 |
| Tesseract 配置 | `detect_num_gps.py`: `OCR_PSM=7`, `OCR_OEM=1`, `OCR_WHITELIST="0123456789"`, `num_conf=80` | 单行文本模式 + 仅识别数字 + 置信度过滤 |
| 数字中心点坐标反算 | `detect_num_gps.py` 中 `run_cls_batch()`: `cx_sub = int(xk * 0.634 + zjjg[0][0] * 0.366)` | 关键点加权 → 子图中心 → 整图坐标的三级映射 |
| 分块流式处理策略 | `detect_num_gps.py` 中 `IMG_CHUNK=12`, `process_chunk()` | 内存受限下的分批处理与显存释放 |

---

## 3.4 天井定位方法

| 子要点 | 对应代码 | 说明 |
|--------|----------|------|
| 定位所需数据源 | `save_pic.cpp`: GPS（`plane_lat/lon`）、姿态角（`plane_pitch/yaw/roll`）、相对高度（`plane_alt`） | 数据从 MAVROS 获取，通过共享内存传递 |
| 简化版定位模型 | `detect_well(5).py` 中 `get_well_gps()`: 仅 pitch + yaw 补偿 | 小角度假设下的近似解算 |
| 完整版定位模型 | `detect_num_gps.py` 中 `get_well_gps()`: pitch + yaw + roll 全姿态补偿 | 含旋转矩阵的严格几何解算 |
| 像素 → 相机坐标系 | 两文件均包含: `xp = x_pic - img_cols/2`, `zc = alt / (-cos(pitch))`, `xc = -zc * xp * (cell_width / img_cols)` | 针孔相机模型的逆投影 |
| 相机 → ENU 世界坐标系 | `detect_num_gps.py`: 3×3 旋转矩阵 $R = R_z(\text{yaw}) \cdot R_y(\text{pitch}) \cdot R_x(\text{roll})$ | 完整旋转补偿 |
| ENU → GPS 经纬度 | 两文件均包含 `enu_meters_per_deg()`: WGS84 椭球下每度对应的米数 | 纬度相关的动态换算 |
| 沿航向 / 垂直航向的 scale 修正 | `detect_num_gps.py`: `along *= scale_gps` 的经验修正 | 补偿系统误差 |

---

## 3.5 多帧融合定位

| 子要点 | 对应代码 | 说明 |
|--------|----------|------|
| 井的聚类策略 | `detect_well(5).py` 中 `update_wells()`: 10m 阈值 | 基于欧氏距离的单阈值聚类 |
| 改进的距离分层聚类 | `detect_num_gps.py` 中 `update_wells()`: `dis_same_well=5m`, `dis_dfrt_well=10m` | 相同井合并 vs 新井判定的双阈值策略 |
| 聚类中心的动态更新 | `detect_num_gps.py`: 每次合并后重新计算 `avg_lon / avg_lat` | 滑动平均式的位置精化 |
| 按类别 / 数字的属性累积 | `detect_well(5).py`: `per_class[cls_id]`; `detect_num_gps.py`: `per_num[num_result]` | 多帧投票机制的数据结构 |
| 多帧检测次数统计 | 两文件均在 `choose_and_send()` 中统计 `counts` | 检测频次作为置信度权重 |

---

## 3.6 坐标解算方法

| 子要点 | 对应代码 | 说明 |
|--------|----------|------|
| 针孔相机成像模型 | `get_well_gps()` 中的几何变换 | 从像素坐标反推世界坐标的数学推导 |
| 相机内参 | `cell_width=1.263158`, `cell_height=0.947368`, `img_cols=1920`, `img_rows=1080` | 像元尺寸与分辨率参数 |
| ⚠️ 相机标定过程 | **信息不足** — 代码中仅给出 `cell_width/height` 数值，未说明来源 | 需补充：标定方法（张正友标定法）、内参矩阵 $K$、畸变系数 |
| ⚠️ 相机安装参数 | **信息不足** — 代码假设相机光轴垂直向下，未考虑安装角 | 需补充：相机与机体的安装偏角、IMU-相机外参标定 |
| GPS/IMU 数据的时间同步 | `save_pic.cpp` 中每帧同时记录 GPS + 姿态 + 图像 | 硬件同步方案说明 |
| 坐标转换误差分析 | `scale_gps=0.9` 的经验修正因子 | 需补充：误差来源分析与精度评估 |

---

## 3.7 目标决策

| 子要点 | 对应代码 | 说明 |
|--------|----------|------|
| 基于分类计数的决策（第一轮） | `detect_well(5).py` 中 `choose_and_send()`: `stats.sort(key=lambda x: x[3], reverse=True)` | 选取分类置信度最高的井作为目标 |
| 基于数字中位数的决策（第二轮） | `detect_num_gps.py` 中 `choose_and_send()`: Top-3 按检测次数排序 → 按数字大小排序 → 取中位数 | 三选一的稳健决策策略 |
| 结果通过 Socket 发送 | 两文件中的 `send()` 函数: TCP 三步握手（lat → lon → 确认） | 与飞控 / 地面站的通信协议 |
| 进程退出信号 | `detect_well(5).py`: `os.kill(pid, signal.SIGUSR1)` → `save_pic.cpp`: `shutdown_handler` | 识别完成后的优雅退出机制 |

---

## 各节代码支撑度一览

| 章节 | 支撑状态 | 备注 |
|------|:--------:|------|
| **3.1** 侦察流程设计 | ✅ 充分 | 三份代码完整覆盖整体流程 |
| **3.2** 第一轮图片识别方案 | ✅ 充分 | `detect_well(5).py` 覆盖全部细节 |
| **3.3** 第二轮数字识别方案 | ✅ 充分 | `detect_num_gps.py` 覆盖全部细节 |
| **3.4** 天井定位方法 | ✅ 充分 | 两个 `.py` 提供简化版 + 完整版双方案 |
| **3.5** 多帧融合定位 | ✅ 充分 | 两个 `.py` 提供单阈值 + 双阈值双方案 |
| **3.6** 坐标解算方法 | ⚠️ 部分不足 | 缺少相机标定过程、安装参数、误差分析的详细说明 |
| **3.7** 目标决策 | ✅ 充分 | 两套决策策略均可从代码中提取 |

---

## ⚠️ 3.6 节需额外补充的内容（信息不足）

1. **相机内参标定**：`cell_width` / `cell_height` 的来源（标定板尺寸、角点提取、张正友标定法流程、重投影误差）
2. **畸变矫正**：当前代码未见径向 / 切向畸变矫正，需说明是否已做硬件矫正或畸变可忽略
3. **相机-IMU 外参**：相机光轴与机体坐标系的安装偏角，当前代码假设光轴垂直向下
4. **GPS 天线杆臂补偿**：GPS 天线与相机光心的位置偏移
5. **定位误差传递分析**：从像素误差 → 角度误差 → GPS 误差的定量传播链
6. **`scale_gps=0.9` 经验修正因子的来源**：是通过地面标定还是飞行试验确定的
